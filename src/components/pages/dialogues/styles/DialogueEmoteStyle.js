import { blue } from '@material-ui/core/colors';

export const styles = theme => ({
    emoteContainer: {
        backgroundColor: '#222'
    },
    emoteImage: {
        width: 64,
        height: 64,
        padding: 8,
        backgroundColor: '#fff',
        margin: 0
    },
    emoteTarget: {
        color: blue[200],
        fontWeight: 'bold'
    },
    toolbarButton: {
        color: '#fff'
    }
});