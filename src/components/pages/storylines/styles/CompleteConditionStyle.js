import { blue, green } from '@material-ui/core/colors';

export const styles = theme => ({
    conditionContainer: {
        padding: 16,
        width: '100%'
    },
    interactionTypeText: {
        color: blue[400]
    },
    greenText: {
        color: green[400],
        fontWeight: 'bold'
    },
    summaryWrapper: {
        display: 'flex',
        width: '100%'
    },
});